# 更新记录

本项目遵循“面向用户的变化优先”记录方式。日期使用北京时间。

## [Unreleased]

- Agent：新增“指定简历 → 定制面试题”工作流；未选择简历时展示中文选择卡，选择后使用可信 `resume_id` 生成 5 道题，不回显简历正文。修复简历标题中的数字覆盖真实选择 ID 的问题。
- Agent：明确双模式策略。确定性任务始终本地优先；模型只增强开放问题和完整简历深度改写。Agent 界面把“等待补充”改为“选择简历后继续”，补齐中文工具标签和双模式说明。
- 文档：新增 `docs/AGENT_MODES.md`，并修正页面配置 Key 的真实本机保存边界、22 个工具数量和双模式使用建议。
- 展示：新增真实桌面“选择简历 → 定制面试题”运行截图，同步更新 GitHub Pages 和个人网站项目卡。
- Agent：增强无 API Key 本地策略，支持常见自然问法、确定性多工具规划和求职诊断/机会/面试/简历结果综合，不再只返回单个工具文本或能力提示。
- 展示：恢复原电影感 GitHub Pages 设计、背景视频、双主题素材与玻璃视觉，在原结构上加入真实 1440 × 900 Agent 桌面 Web 截图、分层记忆与自研 Runtime 说明；移动端只用于兼容性验收，不再作为项目主图。
- 展示：重排为“完整求职系统 → Agent 核心特色 → 实现原理 → 真实产品界面”，新增四类 Agent 任务、七段 Runtime 链路和记忆分层说明，并准确区分自研有界 Tool-Calling Runtime、标准 ReAct 与 LangChain/LangGraph。
- 启动：重复双击 `start.bat` 会健康检查并复用已运行页面；真正失败时保留窗口和日志；只读 `-Diagnostics` 可在服务运行时独立执行。
- 安全：阻止上传目录穿越读取，旧版简历与训练接口统一固定本地用户，手动服务拒绝非回环绑定并关闭 Flask 调试器。
- 兼容：Lucide 与 Chart.js 固定版本并随仓库本地提供；依赖不可用时图标和图表降级，不阻断 Agent 与核心求职流程。
- 测试：Python 332/332、Node 静态交互测试；Chromium 与 Edge 真实流程通过，Firefox 在本机缺少可执行文件时明确记为 SKIP。

- 后续版本应在此记录新增、变更、修复、安全和兼容性变化，并在发布时移动到带日期版本。

## [2.0.0] - 2026-07-12

### 新增

- 全局上下文 Agent：桌面右侧抽屉、移动端底部面板、模块/机会/简历上下文标签。
- 确认式 Agent 操作：持久化预览、允许字段编辑、确认、取消、过期、幂等执行和结果回执。
- 职业档案、机会工作区、简历版本、行动项、领域事件和职业报告服务。
- 分层持久记忆：会话、滚动摘要、确认画像、任务槽位、情景结果与实时业务快照。
- FTS5 优先、有限回退的记忆检索，以及由精确领域事件驱动的行动完成。
- 可恢复且幂等的面试会话，刷新或服务重启后可继续。
- 证据加权准备度，显示组件证据、时间、上限和阻塞项。
- Chromium、Firefox、已安装 Edge 的桌面/移动端到端流程与浏览器能力单元测试。
- 安全便携 Windows 启动器，支持自动端口、`-NoBrowser`、`-Port`、`-SkipInstall` 和 `-Diagnostics`。
- 基于真实产品截图的 GitHub Pages 展示页和资源审计。

### 变更

- 投递状态统一为可迁移的规范阶段；未知历史状态保留在“待确认”。
- Agent 工具改为调用领域服务，不再复制业务写入逻辑。
- 页面与 Agent 之间只传用户输入和实体 ID，服务端重建权威上下文。
- 语音识别、录音和 MIME 选择改为能力检测，始终保留文字与上传路径。
- 数据库改为版本化迁移，并为适用的旧库创建 WAL 一致备份。

### 修复

- 修复机会工作区浏览器前进/后退、错误重试、陈旧请求和跨机会内容串联问题。
- 修复 Agent 请求竞态、提案恢复、移动端布局溢出和关闭后的焦点恢复。
- 修复启动器误伤端口占用进程、重复实例和浏览器禁止端口问题。
- 修复展示页失效资源、过时能力描述和超大重复图片。

### 安全与隐私

- Agent 写入必须由用户确认；确认端点从数据库重新加载参数。
- 固定本地用户并拒绝客户端冒充其他用户。
- 默认仅监听回环地址，确认接口限制同端口本地来源。
- 网页抓取拒绝回环和内网地址并限制响应大小。
- 发布门禁禁止跟踪 Key、数据库、上传简历/音频、导出、日志和浏览器测试产物。

## [1.0.0] - 2026-07-11

- 初始 Flask + SQLite + 原生前端求职辅助系统。
- 提供简历、JD 匹配、面试训练、投递看板、多模型接口和基础 Agent 会话。

[Unreleased]: https://github.com/tl66666/ztai/compare/v2.0.0...HEAD
[2.0.0]: https://github.com/tl66666/ztai/releases/tag/v2.0.0
[1.0.0]: https://github.com/tl66666/ztai/releases/tag/v1.0.0
