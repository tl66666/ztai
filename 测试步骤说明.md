# 测试步骤说明

测试体系已集中维护，避免多个文档中的命令和接口清单逐渐不一致：

- 面向开发和发布：[完整测试与发布指南](docs/TESTING.md)
- 面向功能体验和演示：[用户指南](docs/USER_GUIDE.md)
- 面向 Agent、记忆和数据边界：[架构说明](docs/ARCHITECTURE.md)

最小回归命令：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
node --check static/js/app.js
git diff --check
```

发布前还必须执行 JavaScript 单元测试、Windows 干净路径启动 smoke 和可用浏览器的 Playwright 矩阵，具体命令以 `docs/TESTING.md` 为准。
